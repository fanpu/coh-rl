# Final review findings — sim core (reviewer adb9c39312c258926) + controller rulings

## Important (must fix)
1. coh/sim/orders.py ~:305 — `Retreat` raises KeyError when the player's HQ no longer exists (validate_retreat never checks player.hq_id in state.buildings). Fix: retreat target = the player's HQ if alive, else the nearest complete building of the squad's TEAM with reinforce_radius > 0 (reuse orders.reinforce_building's scan), else invalid OrderResult. Test: HQ destroyed → Retreat returns ok=False (or retreats to a teammate's HQ in a 2-players-one-team sim), never raises.
2. coh/sim/orders.py ~:805 (+ :396) — malformed payloads raise instead of OrderResult(ok=False): Move(sq,(1,2,3)) ValueError; Move(sq,("a","b")) TypeError; Move(sq,5) TypeError; Move(sq,(1.5,2.5)) IndexError inside pathfinding; Capture(sq, []) TypeError unhashable. Fix: a payload-shape gate at the top of validate_order (cell must be a 2-tuple/list of ints — accept list and normalize to tuple since JSON gives lists; ids must be int (bool excluded); point_id/unit/upgrade/structure must be str; direction_deg must be a finite real). Add an order-payload FUZZ test asserting sim.issue() never raises over a matrix of malformed payloads for every order type, including via order_from_dict on hostile dicts (missing keys, extra keys, unknown "type", wrong types) — order_from_dict itself may raise a documented ValueError, but CohEnv/replay callers must not crash on hostile agent output: check coh/env/env.py step() handles a non-Order object in an agent's list by counting it invalid (you may touch coh/env/env.py for that only).
3. coh/sim/orders.py ~:387 — SetFacing(nan/inf) is accepted and permanently breaks state_hash(). Covered by the finite check in #2; add a regression test asserting state_hash() still works.
4. coh/sim/systems/territory.py ~:224 vs coh/sim/sim.py ~:128 — two definitions of a team's HQ sectors (player setups vs map StartDef.team); with two players on one team in slots whose map teams differ, recompute_connectivity silently drops an HQ sector. Fix: compute `sim.hq_sectors: dict[int, list[int]]` once in Sim.__init__ from the player setups; both _setup_players and recompute_connectivity (and territory.hq_point_ids) read it. Test with the two-players-one-team setup used in tests/sim/test_economy.py.

## Controller ruling on the spec-fidelity note (implement)
5. Capture contesting — the SPEC wins over the plan's narrowing: a point is CONTESTED (progress stalls, no decay) whenever eligible infantry of more than one team are inside the capture radius, regardless of their orders. "Eligible presence" = capture_rate > 0, alive members, not abandoned, not RETREATING, not garrisoned (pinned squads still count as presence). PROGRESS is still only made by squads that carry a Capture order for that point and are not pinned (unchanged). Consequence: a defender standing on its own point blocks neutralization without needing a Capture order. Keep validate_capture's rejection of fully-owned points. Update territory tests accordingly.

## Minor (do these — all small)
6. Sim.__init__: run systems.vision.run(self) once at the end so visibility is correct at reset (first-step Attack orders are currently all rejected as "not visible").
7. combat Attack pursuit: a target that exists and is visible but can never be engageable (emptied neutral building; entity now on own team via re-crew) must clear the Attack order instead of re-pathing forever.
8. pathfinding path cache: add a FIFO cap constant (PATH_CACHE_MAX) like VISION_MASK_CACHE_MAX.
9. production._spawn_trained_squad: when no rally cell exists, do not silently eat the unit — keep the item at the head of the queue and retry next tick (emit Event "unit_blocked" once per item).
10. destruction._abandon_weapon: clear the shell's `upgrades` (and any pending upgrade) so a re-crewing enemy doesn't inherit them.
11. Sim.issue after state.winner is set: reject all orders with OrderResult(False, "game over") and do not count them as invalid; Sim.tick on a finished game should still clear stale events exactly once (or leave as is if the viewer/frames builder relies on it — check coh/viewer/frames.py first).

## Rules for this fix wave
- FIRST `git merge --no-edit main`. Touch only coh/sim/**, tests/sim/**, tests/scenarios/**, tests/helpers.py, and the single env.py robustness tweak named in #2. The viewer agent is concurrently editing viewer/** and tests/viewer/** — stay out.
- Items 5, 6, 7, 9 legitimately change simulation behaviour, so tests/sim/test_golden_hashes.py values (and the stress-state hash oracle from the bench task, wherever it lives) WILL change. Procedure: do all non-behavioural fixes first (1–4, 8, 10, 11) and confirm golden hashes are UNCHANGED → commit. Then do 5, 6, 7, 9, re-record the golden values in a separate commit whose message explains exactly which rulings changed behaviour.
- If viewer's event-kind coverage test (tests/viewer/test_effect_coverage.py) fails because you added a new Event kind ("unit_blocked"), add the minimal entry to viewer/viewer.js's EFFECTS table ONLY if the viewer agent has already merged to main; otherwise name the event in your report and leave that one test failing, stating so explicitly.
- Full suite otherwise green. Conventional commits ending with: Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
- Report to /home/fzeng/ml/coh/.superpowers/sdd/2026-09-19-m1-playable-sim/final-fix-sim-report.md (write in your worktree and cp if the absolute path is refused). Reply with status, branch, commit SHAs, test summary, concerns.
