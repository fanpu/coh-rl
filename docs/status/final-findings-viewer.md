# Final review findings — viewer (reviewer a6f68bd0808cf7c86)
1. (Critical) viewer/viewer.js drawBuildings (~:628) never fog-filters: in team-fog mode enemy buildings (position, HP, construction, inspector queue) are fully visible. Thread the visibility bitmap in as drawSquads does (footprint cells); in team view show enemy buildings only if currently visible, else — if previously seen by that team at an earlier frame — a greyed "last known" ghost with no live HP/queue; never-seen → hidden. Inspector must not reveal live data of non-visible enemy entities.
2. (Important) Effects ignore fog: tracers/explosions/death marks etc. in cells not visible to the selected team are drawn. Add isVisibleWorld(x,y,bits); skip effects whose primary position is not visible (tracers: draw only if the source OR target end is visible, clipped to the visible end being fine).
3. (Important) test_fog_and_cover_toggles only checks state flags/non-blank canvas. Add a test that an enemy building and an enemy-only effect outside team vision are actually not rendered in team-fog mode (pixel sample/digest at the known screen position, or a render-count hook exposed via window.__viewer).
4. (Minor) comment that tests/viewer/conftest.py deadline() is not nesting-safe.
