# Final review findings — data / maps / env / agents / replay / bench (reviewer a3c652bf95c401ee8) + controller rulings

## Critical
1. coh/maps/library/hedgerow_crossing.yaml is NOT seat-symmetric: terrain, sectors and neutral buildings are exact 180° rotations, but POINT CELLS are not (grid centred on (47,47)/(43,43) instead of (47.5,47.5)). Infantry BFS distance HQ→point: victory_mid 58 vs 74 cells, victory_north/south 34 vs 40, fuel_high/munitions_high 30 vs 38. Same bot, same faction, both seats: seat 0 won 14/16 mirror matches. Fix: every point's cell must be the exact rotational image (x,y)→(W−1−x,H−1−y) of its partner's; a single centre point is impossible on an even grid, so victory_mid must be equidistant BY PATH from both HQs (put it on cells such that BFS distance from both HQ spawn areas is equal — verify by BFS — or replace it by a rotationally paired arrangement, e.g. keep 3 VPs as north/south pair + a mid VP whose capture cell is one of the 4 centre cells AND whose sector is symmetric; assert |dist0 − dist1| ≤ 1 cell). Also correct `hq_cell` values so the two HQ placements are exact rotational images under the sim's footprint-anchoring rule (the sim agent is changing the rule to "footprint centred on hq_cell", see its findings file final-findings-sim.md addendum: for an even footprint the hq_cell is the cell just below-right of the footprint centre for every seat — coordinate with that: choose hq_cells so the STAMPED FOOTPRINTS are exact rotational images; verify in a test by computing footprints with the same formula).
   Tighten tests/maps/test_hedgerow_crossing.py: assert point cells are exact rotational images (mid VP: path-distance equality), hq footprints rotational images, and BFS distances from each HQ to each point's rotational partner are equal (±1).

## Important
2. (Challenges an earlier controller ruling — accepted.) The fuel_high / munitions_high "swap pair" is unfair: fuel ≫ munitions in value. RULING: strict type symmetry — every point's rotational partner has the SAME type. Redesign the point types on the existing 13 sectors (or add sectors if you must, keeping exact symmetry): each side must have access to the same resource mix; keep 3 victory points; keep a contested high-value centre. Suggested: centre-west & centre-east both `fuel_high`?? is too fuel-rich — prefer centre pair = `munitions_high` ×2 and convert one existing per-side pair to `fuel_med`/`fuel_high` so per-side totals look like a CoH 1v1 map (roughly: near base 1 strategic + 1 fuel_low + 1 munitions_med; mid-field 1 fuel_med/high + 1 munitions_high shared-ish; 3 VPs). Document the final layout in the YAML header comment.
4. coh/env/observation.py ~:400 — PointView.has_op leaks enemy observation posts through fog. Gate it: true OP state only if the OP building is visible to the team (or the point is team-owned); otherwise last-known from the team's ghosts, else False.
5. coh/env/observation.py ~:359 — neutral-building hp_frac is live outside vision. Report last-known (track per-team last-seen hp for neutral buildings in the observation layer's own per-env memory, or simply 1.0 when never seen) when not visible.
6. coh/data/loader.py — validation is far thinner than the sim assumes; 15/15 malformed tables were accepted. Extend _cross_validate (pass it economy + neutral): enum membership for `kind` (infantry/team_weapon/vehicle) and `faction` (us/wehr); vehicles members == 1; members ≥ 1; team_weapon slot 0 must be a weapon with setup_time > 0 (or at least non-empty); non-negative costs/hp/times; footprint ≥ 1×1; `ranges` strictly ascending and min_range < ranges[2]; accuracy/penetration within [0, +∞) and cover/target mods ≥ 0; every squad/building/neutral target_type appears in at least one weapon target_table OR is explicitly listed in a new optional top-level `generic_target_types:` list in weapons.yaml (so identity fallback is deliberate); economy.op_building[faction] ∈ buildings for every faction that has buildings; economy.point_income contains every point type the map format knows (strategic, munitions_low/med/high, fuel_low/med/high, victory); each faction with an HQ has ≥ 1 builder squad producible (HQ produces a squad with non-empty `builds`) and ≥ 1 reachable building producing a capture-capable squad; no cycles in `requires`. One test per rule (parametrized table of malformed snippets → DataError naming file + key). The fixtures must still load — fix fixtures additively if a new rule rejects them (and say so).
7. coh/replay/replay.py — a replay records no identity for data or map, and final_hash is never verified outside tests. Add `data_hash` (sha256 over a canonical dump of the loaded GameData) and `map_hash` (name + terrain + sectors + points + starts + neutral placements) to the replay (version stays 1 if fields are optional; bump to 2 if you make them required — then keep loading v1); `resimulate(..., verify=True)` (default True) raises ReplayMismatchError before simulating if the supplied data/map hashes differ from recorded ones, and after the last tick if the final state_hash differs. Put `data_hash(data)` in coh/data (loader or a small hashing module) and `map_hash(game_map)` in coh/maps. coh/viewer is owned by another agent right now: do not edit it — just make sure its existing call `resimulate(replay, data=..., game_map=...)` keeps working (verification on by default is fine; if the viewer passes a map object make sure hashing a loaded GameMap is stable).

## Minor (do these)
8. coh/maps/format.py ~:278 — `cell_m` parsed then discarded: raise MapError unless it equals the sim's CELL_M (2.0) (keep the dependency direction: compare against the literal default already in format.py and note it).
9. coh/maps/format.py ~:394 — validate: no duplicate start `slot`; each start's `sector` equals sector_id at hq_cell; point `type` ∈ the known point-type set.
10. coh/replay/replay.py to_dict — use dataclasses.asdict(self.config).
11. coh/replay — wrap malformed/truncated replay problems in `ReplayError` naming the offending entry (load and resimulate); test with hostile files.
12. coh/env/env.py rewards — pay the terminal reward exactly once (the step on which the game ends); subsequent no-op steps return 0.0. Update the test that asserted otherwise. Document in README.
13. coh/env/env.py ~:111 — neutralize first-mover advantage: rotate the per-step order-issue sequence across players by step index (deterministic: start index = step_count % n_players, then ascending id cyclically). Order log must keep recording in ACTUAL issue order so replays reproduce it. Also: an agent returning a non-Order object (or a dict) in its list must not raise — count it as an invalid order in infos and skip it (dicts may be parsed with order_from_dict inside a try).
14. Hand each agent its own copies: `game_map.copy()` and a deep-copied GameData in scripts/play_match.py and the ladder/bot test harness (or, better, do it once inside a small `run_match` helper in coh/env that both use — if one already exists in tests, promote it to coh/env/match.py and make scripts/play_match.py + tests use it).
15. scripts/bench_sim.py — add warmup + best-of-N (default 3) and state in the README that numbers are measured with the per-system timers installed.
16. README — quickstart must work today: add `--data-dir tests/data/fixtures`; call coh/env "PettingZoo-style parallel multi-agent env", not Gym-style.
18. Tests: a `slow` SEAT-FAIRNESS test — same bot (T1), same faction, both seats, ≥ 8 seeds × 2 factions on hedgerow_crossing with fixture data and a 1200 s limit: neither seat wins more than 12/16 (generous bound; report the actual split). NOTE: full fairness also needs the sim-side fix (HQ footprint centring + builder spawn toward map centre) being done concurrently by another agent in coh/sim/sim.py — DO NOT edit coh/sim. If your fairness test still fails only because that sim fix isn't in your branch yet, mark it `xfail(strict=False, reason=...)` and say so; the controller will remove the xfail after merging both branches. Also note seeds are nearly degenerate for T1 mirror play (wehr mirror gave byte-identical outcomes over 8 seeds): make the fairness harness perturb the opening (e.g. a seeded random 0–10 s delay before each bot's first orders, implemented in the TEST harness as a wrapper agent, not in T1).

## Deferred by controller (do NOT do): 17 (viewer assets not in the wheel) — handled in the upcoming viewer graphics pass.

## Rules for this fix wave
- FIRST `git merge --no-edit main`. Touch only: coh/data/**, coh/maps/**, coh/env/**, coh/agents/**, coh/replay/**, coh/bench/**, scripts/**, README.md, tests/data/**, tests/maps/**, tests/env/**, tests/agents/**, tests/replay/**, tests/scenarios/test_ladder.py + a new tests/scenarios/test_seat_fairness.py, tests/data/fixtures/** (additive). NOT coh/sim/**, NOT viewer/**, NOT coh/viewer/**, NOT tests/sim/**, NOT tests/viewer/**.
- The map change and the issue-order rotation legitimately change match outcomes, so tests/sim/test_golden_hashes.py (and the bench stress-hash oracle if it depends on the map) will fail in your branch. Do NOT edit those files (another agent is editing them); list the failing golden tests in your report — the controller re-records them after merging. Every other test must be green.
- Work test-first; reproduce each reviewer claim before fixing; if a claim doesn't reproduce, say so.
- Conventional commits (group logically: map fairness / fog leaks / loader validation / replay identity / env+minors), each ending with: Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
- Report to /home/fzeng/ml/coh/.superpowers/sdd/2026-09-19-m1-playable-sim/final-fix-env-report.md (write in your worktree and cp if the absolute path is refused): include the before/after seat-fairness split and BFS distance table. Reply with status, branch, commit SHAs, test summary (naming expected golden failures), concerns.
